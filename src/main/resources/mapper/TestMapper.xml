<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--  查询条件集 -->
<mapper namespace="com.ancestors.manage.dao.TestDao">
      <select id="selectEntityById" resultType="com.ancestors.manage.entity.TestEntity" parameterType="int">
            select * from test where id=#{id}
      </select>
</mapper>
